{
  "name": "ng-log-pretty",
  "version": "1.0.1",
  "description": "A decorator for Angular's $log service that adds a \"pretty\" method that will pretty print XML, JSON, CSS, and SQL.",
  "main": "angular-log-pretty.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike Tierney <mike.tierney@intridea.com>",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-concat": "^2.5.2",
    "gulp-karma": "0.0.4",
    "gulp-rename": "^1.2.0",
    "gulp-uglify": "^1.1.0",
    "jasmine-core": "^2.2.0",
    "karma": "^0.12.31",
    "karma-jasmine": "^0.3.5",
    "karma-mocha-reporter": "^1.0.2",
    "mocha": "^2.2.1"
  }
}
